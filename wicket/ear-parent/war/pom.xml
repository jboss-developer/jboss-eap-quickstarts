<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <parent>
        <groupId>org.jboss.as.quickstart.wicket.ear</groupId>
        <artifactId>as7-quickstart-wicket-ear-parent</artifactId>
        <version>1.0.0</version>
    </parent>
    
    <groupId>org.jboss.as.quickstart.wicket.ear</groupId>
    <artifactId>as7-quickstart-wicket-ear-war</artifactId>
    <packaging>war</packaging>
    
    <name>JBoss AS Quickstarts: Wicket EAR: WAR</name>
    
    <build>
        <resources>
            <resource>
                <filtering>false</filtering>
                <directory>src/main/resources</directory>
            </resource>
            <!-- Pack .html files next to source files as resources. -->
            <resource>
                <filtering>false</filtering>
                <directory>src/main/java</directory>
                <includes><include>**</include></includes>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </resource>
        </resources>
    </build>
   
    <dependencies>
        
        <!-- Java EE 6 API. -->
        <dependency>
            <groupId>javax</groupId>
            <artifactId>javaee-api</artifactId>
            <version>6.0</version>
            <scope>provided</scope>
        </dependency>
        
        <!-- Wicket Java EE integration. -->
        <dependency>
            <groupId>org.wicketstuff</groupId>
            <artifactId>wicketstuff-javaee-inject</artifactId>
            <version>1.5.4</version>
        </dependency>
        
        <!-- EJB JAR. -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>as7-quickstart-wicket-ear-ejbjar</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
    
</project>
